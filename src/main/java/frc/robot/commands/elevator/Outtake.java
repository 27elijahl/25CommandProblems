package frc.robot.commands.elevator;

import edu.wpi.first.wpilibj2.command.Command;

/**
 * A command which takes a coral currently stored in the 
 * End Effector and outputs it through the front intake. 
 * This command ends once the coral is no longer in the 
 * End Effector (when it can no longer be dectected by 
 * either sensor). You do not need to write this command.
 */
public class Outtake extends Command
{
    
}
