package frc.robot.commands.elevator;

import edu.wpi.first.wpilibj2.command.*;

/**
 * An indefinite command which intakes a coral from the back 
 * intake as long as a coral is detected there (when there is
 * no coral in the subsystem, do nothing). Intakes the coral
 * until it is detected by both sensors, meaning that it is
 * in the middle of the subsystem. You do not need to write
 * this command.
 */
public class Intake extends Command
{
}
